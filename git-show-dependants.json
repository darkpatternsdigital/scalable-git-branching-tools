{
    "local": [
        {
            "id": "get-dependants",
            "type": "get-dependants",
            "parameters": {
                "recurse": "$params.recurse",
                "target": "$params.target"
            }
        }
    ],
    "finalize": [],
    "output": "$actions['get-dependants'].outputs"
}
